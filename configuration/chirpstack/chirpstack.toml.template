[logging]
  level="info"

[postgresql]
  dsn="postgres://${POSTGRES_USER}:${POSTGRES_PW}@postgresql/${POSTGRES_DB}?sslmode=disable"
  max_open_connections=10
  min_idle_connections=0

[redis]
  servers=[
    "redis://redis/",
  ]
  tls_enabled=false
  cluster=false

[network]
  net_id="000000"
  enabled_regions=[
    "eu868",
    "us915_1",
  ]

[api]
  bind="0.0.0.0:8080"
  secret="${CHIRPSTACK_API_SECRET}"

[integration]
  enabled=["mqtt"]
  [integration.mqtt]
    server="tcp://mosquitto:1883/"
    json=true
